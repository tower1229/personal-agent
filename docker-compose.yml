services:
  personal-agent:
    build: .
    container_name: personal-agent
    env_file:
      - .env
    environment:
      ADMIN_HOST: 0.0.0.0
    ports:
      - "127.0.0.1:${ADMIN_PORT:-3000}:${ADMIN_PORT:-3000}"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
